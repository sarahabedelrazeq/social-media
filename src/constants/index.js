import {
  DEFAULT_LANGUAGE,
  DEFAULT_USER_IMAGE,
  DEFAULT_THEME,
  DEFAULT_COLOR,
  PERSIST_KEY,
} from "./config";
import IMAGES from "./images";

export * from "./regex";
export {
  DEFAULT_USER_IMAGE,
  DEFAULT_LANGUAGE,
  IMAGES,
  DEFAULT_THEME,
  DEFAULT_COLOR,
  PERSIST_KEY,
};
